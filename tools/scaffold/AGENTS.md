# Agent Instructions

You are the coordinator for this limina project.

Boot the editor with `npm run editor`, register the printed `limina` bridge as an
MCP server in your coding agent, then follow `COORDINATOR.md`.
