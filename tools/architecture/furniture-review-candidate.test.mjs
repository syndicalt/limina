import assert from "node:assert/strict";import fs from "node:fs";import {validateBuildingStageArtifact} from "../../js/src/assets/staged-building-pipeline.mjs";
const path=process.argv[2];if(!path)throw new Error("usage: node tools/architecture/furniture-review-candidate.test.mjs <candidate.json>");const artifact=validateBuildingStageArtifact(JSON.parse(fs.readFileSync(path,"utf8")));
assert.equal(artifact.kind,"furniture-pack");assert.equal(artifact.status,"candidate");assert.equal(artifact.evidence.length,5);assert.equal(artifact.metadata.humanDecision,"pending");assert.deepEqual(artifact.evidence.map(entry=>entry.evidenceId.split("/").at(-1)),["front","right-side","back","three-quarter","joinery-detail"]);console.log(`furniture-review-candidate OK: ${artifact.artifactId}, ${artifact.evidence.length} exact PNG hashes awaiting F1 HITL`);
