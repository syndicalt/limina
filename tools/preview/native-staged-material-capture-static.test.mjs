import assert from "node:assert/strict";
import { readFileSync } from "node:fs";
import test from "node:test";
const runner=readFileSync(new URL("./run-native-staged-material-capture.mjs",import.meta.url),"utf8"),demo=readFileSync(new URL("../../js/src/demos/staged_material_capture_window.ts",import.meta.url),"utf8");
test("M1 production capture is native, canonical, fixed, and engine-mounted",()=>{assert.match(runner,/staged_material_capture_window\.ts/);assert.match(runner,/poly-haven-pack-swatches,authored-simple-role-swatches,representative-exterior-shell-crop,representative-interior-hearth-crop/);assert.match(runner,/minimumWidth<1920\|\|minimumHeight<1080/);assert.match(demo,/mountStagedMaterialReview/);assert.match(demo,/fixedTimeSeconds/);assert.match(demo,/neutral:true/);});
test("M1 capture is fail-closed around Xid and every timestamp environment variable",()=>{assert.match(runner,/current boot already contains an NVIDIA Xid/);assert.match(runner,/journalctl.*-f.*-n.*0/s);assert.match(runner,/setInterval\(.*kernelLog.*250/s);assert.match(runner,/must not be retried before reboot/);assert.match(runner,/NVIDIA Xid detected after capture/);assert.match(runner,/delete captureEnv\.LIMINA_GPU_TIMESTAMP_RISK_ACK/);assert.match(runner,/delete captureEnv\.LIMINA_GPU_TIMESTAMP_MODE/);assert.match(runner,/delete captureEnv\.LIMINA_GPU_TIMESTAMP_QUERIES/);assert.match(runner,/for\(const key of Object\.keys\(captureEnv\)\)if\(\/TIMESTAMP\/i\.test\(key\)\)delete captureEnv\[key\]/);assert.match(runner,/gpuTimestampMode!=="disabled"/);assert.match(runner,/timestampQueriesEnabled!==false/);});
test("M1 runner validates telemetry and writes only private artifacts after deleting trace",()=>{assert.match(runner,/single-production-frame-all-passes/);assert.match(runner,/paired-render-submission\/v1/);assert.match(runner,/renderer-live-after-production-frame/);assert.match(runner,/cpu-pixel-exposure\/v1/);assert.match(runner,/assets\/qc\/internal\/materials\/functional-hall-house-v4/);assert.match(runner,/unlink\(tracePath\)/);assert.match(runner,/chmod\(reviewRoot,0o700\)/);assert.match(runner,/chmod\(path,0o600\)/);assert.match(runner,/capture-evidence\.json/);assert.doesNotMatch(runner,/0\.0\.0\.0|http\.createServer|--serve/);});
test("M1 revised captures select exact authority and publish append-only",()=>{assert.match(demo,/op_read_env\("LIMINA_STAGED_MATERIAL_AUTHORITY"\) \|\| DEFAULT_AUTHORITY_PATH/);assert.match(runner,/LIMINA_STAGED_MATERIAL_AUTHORITY:authorityPath/);assert.match(runner,/value\("--authority",defaultAuthorityPath\)/);assert.match(runner,/value\("--out-dir",defaultReviewPath\)/);assert.match(runner,/revised staged material capture requires an explicit fresh --out-dir/);assert.match(runner,/revised staged material output already exists/);assert.match(runner,/flag:appendOnly\?"wx":"w"/);assert.match(runner,/roof-dormer-eave-continuity/);});
