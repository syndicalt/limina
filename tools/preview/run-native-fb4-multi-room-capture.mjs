import { createHash, randomUUID } from "node:crypto";
import { spawn, spawnSync } from "node:child_process";
import { constants } from "node:fs";
import { chmod, lstat, mkdir, open, readFile, rename, rm, unlink, writeFile } from "node:fs/promises";
import { dirname, isAbsolute, relative, resolve, sep } from "node:path";
import { fileURLToPath } from "node:url";
import sharp from "../../js/node_modules/sharp/lib/index.js";
import { buildFb4CaptureProvenance } from "../../js/src/render/fb4-capture-provenance.ts";
import { portableAssetContentHash } from "../../js/src/world/asset-content-hash.mjs";
import { buildCaptureProducerClosure } from "./capture-producer-closure.mjs";

export const FB4_CAPTURE=Object.freeze({binary:"target/release/limina",module:"js/src/demos/building_multi_room_review_window.ts",sourcePaths:Object.freeze(["js/src/render/building-multi-room-review-scene.ts","js/src/demos/building_multi_room_review_window.ts","tools/preview/run-native-fb4-multi-room-capture.mjs"]),authorityEnvironment:"LIMINA_FB4_MULTI_ROOM_REVIEW_AUTHORITY",traceEnvironment:"LIMINA_FB4_MULTI_ROOM_REVIEW_TRACE",traceDirectory:"traces",tracePrefix:"fb4-multi-room-native-capture",lock:"traces/fb4-multi-room-native-capture.lock",privateRoot:"assets/qc/internal/fb4-multi-room",reviewRoot:".limina/review-artifacts",reviewBindHost:"127.0.0.1",viewIds:Object.freeze(["exterior-entry","exterior-rear","gable-elevation","frame-entry-window-detail","ground-rooms-passage","stair-opening","upper-room","lod-25m"])});
const root=resolve(dirname(fileURLToPath(import.meta.url)),"../.."),XID=/(?:NVRM:\s*)?Xid\b|Xid \(PCI/i,PNG=Buffer.from([137,80,78,71,13,10,26,10]);
const raw=b=>`sha256:${createHash("sha256").update(b).digest("hex")}`,portable=(repo,path)=>relative(repo,path).split(sep).join("/"),inside=(base,target)=>{const r=relative(base,target);return r!==""&&r!==".."&&!r.startsWith(`..${sep}`)&&!isAbsolute(r);};
const absent=async(path,label)=>{try{await lstat(path);throw new Error(`append-only ${label} already exists: ${path}`);}catch(error){if(error?.code!=="ENOENT")throw error;}};
function kernelLog(){const result=spawnSync("journalctl",["-k","-b","--no-pager","-o","cat"],{encoding:"utf8",timeout:30000,maxBuffer:32*1024*1024});if(result.error||result.status!==0)throw new Error(`cannot establish required Xid guard: ${result.error?.message??result.stderr??result.status}`);return result.stdout;}
function firstXid(text){return String(text).split(/\r?\n/).find(line=>XID.test(line));}
function guarded(command,args,environment,repo){return new Promise((accept,reject)=>{let failure="",settled=false;const monitor=spawn("journalctl",["-k","-f","-n","0","--no-pager","-o","cat"],{cwd:repo,stdio:["ignore","pipe","pipe"]}),child=spawn(command,args,{cwd:repo,env:environment,stdio:["ignore","pipe","pipe"]});const stop=message=>{if(!failure){failure=message;child.kill("SIGTERM");}},finish=error=>{if(settled)return;settled=true;clearInterval(poll);monitor.kill("SIGTERM");error?reject(error):accept();};monitor.stdout.on("data",chunk=>{const event=firstXid(chunk);if(event)stop(`NVIDIA Xid detected; capture stopped and must not be retried before reboot: ${event.trim()}`);});monitor.on("error",error=>stop(`live Xid monitor failed: ${error.message}`));monitor.on("close",(code,signal)=>{if(!settled)stop(`live Xid monitor exited before capture completion: ${code??signal}`);});const poll=setInterval(()=>{try{const event=firstXid(kernelLog());if(event)stop(`NVIDIA Xid detected; capture stopped and must not be retried before reboot: ${event.trim()}`);}catch(error){stop(`live Xid polling failed: ${error.message}`);}},250);child.stdout.on("data",chunk=>process.stdout.write(chunk));child.stderr.on("data",chunk=>process.stderr.write(chunk));child.on("error",finish);child.on("exit",(code,signal)=>finish(failure?new Error(failure):code!==0?new Error(`guarded FB-4 capture exited ${code??signal}`):undefined));});}
function pixels(rgba,width,height){if(rgba.length!==width*height*4)throw new Error("FB-4 RGBA dimensions drifted");let min=255,max=0,black=0,white=0;for(let i=0;i<rgba.length;i+=4){const y=Math.round(.2126*rgba[i]+.7152*rgba[i+1]+.0722*rgba[i+2]);min=Math.min(min,y);max=Math.max(max,y);if(y<4)black++;if(y>251)white++;}const total=width*height;if(max-min<24||black/total>.45||white/total>.12)throw new Error(`FB-4 frame is not reviewable: range=${max-min}, black=${black/total}, white=${white/total}`);return{schema:"limina.rgba-luminance-sanity/v1",minimum:min,maximum:max,dynamicRange:max-min,blackFraction:black/total,whiteFraction:white/total};}
function validateNativeArtifact(artifact,authority){if(artifact.schema!=="limina.fb4-multi-room-native-review-set/v1"||artifact.backend!=="native-webgpu"||artifact.captureClass!=="production-engine"||artifact.timingPolicy?.timestampQueriesEnabled!==false||artifact.timingPolicy?.gpuTimestampMode!=="disabled"||artifact.captures?.length!==authority.evidenceViews.length)throw new Error("FB-4 native trace identity drifted");for(let i=0;i<authority.evidenceViews.length;i++){const expected=authority.evidenceViews[i],capture=artifact.captures[i];if(capture.id!==expected.id||capture.role!==expected.role||JSON.stringify(capture.camera)!==JSON.stringify(expected.camera)||capture.width!==authority.presentation.minimumResolution[0]||capture.height!==authority.presentation.minimumResolution[1])throw new Error(`FB-4 native trace camera/role/resolution drifted: ${expected.id}`);}const envelope=authority.siteReviewEnvelope,proof=artifact.site?.reviewEnvelope,cameras=proof?.cameraEvidence?.views;if(proof?.populationMaximumHorizontalReachM!==envelope.populationMaximumHorizontalReachM||!Array.isArray(cameras)||cameras.map(({id})=>id).join(",")!==authority.evidenceViews.map(({id})=>id).join(","))throw new Error("FB-4 native trace lacks site-review envelope proof");const exterior=new Set(envelope.discretePopulationExclusion.exteriorViewIds),interior=new Set(envelope.cameraChecks.interiorViewIds);for(const camera of cameras){if(camera.minimumTerrainClearanceM<envelope.cameraChecks.minimumTerrainClearanceM||(exterior.has(camera.id)&&(camera.projectedAreaFraction<envelope.cameraChecks.minimumProjectedAreaFraction||camera.projectedHeightFraction<envelope.cameraChecks.minimumProjectedHeightFraction))||(interior.has(camera.id)&&camera.interiorContained!==true))throw new Error(`FB-4 native trace site-review camera failed: ${camera.id}`);}}
export async function runFb4Capture(options={}){const repo=resolve(options.repoRoot??root),authorityPath=options.authorityPath;if(!authorityPath)throw new Error("--authority is required");const authorityAbsolute=resolve(repo,authorityPath);if(!inside(repo,authorityAbsolute))throw new Error("FB-4 authority escaped repository");const authorityBytes=await readFile(authorityAbsolute),{validateMultiRoomReviewAuthority,assertMultiRoomReviewCaptureReady}=await import("../../js/src/render/building-multi-room-review-scene.ts"),authority=assertMultiRoomReviewCaptureReady(validateMultiRoomReviewAuthority(JSON.parse(authorityBytes))),manifestBytes=await readFile(resolve(repo,authority.candidate.manifest.path)),manifest=JSON.parse(manifestBytes);const out=resolve(repo,options.outDir??"");if(!inside(resolve(repo,FB4_CAPTURE.privateRoot),out))throw new Error("FB-4 private output must be a new child of the dedicated private root");await absent(out,"FB-4 output directory");const prefix=options.reviewPrefix;if(!/^[a-z0-9][a-z0-9-]{2,80}$/.test(prefix??""))throw new Error("FB-4 review prefix is unsafe");if(options.fullscreen!==undefined&&typeof options.fullscreen!=="boolean")throw new Error("FB-4 fullscreen option must be boolean");const traceRoot=resolve(repo,FB4_CAPTURE.traceDirectory),lockPath=resolve(repo,FB4_CAPTURE.lock);await mkdir(traceRoot,{recursive:true,mode:0o700});let lock;try{lock=await open(lockPath,constants.O_WRONLY|constants.O_CREAT|constants.O_EXCL|(constants.O_NOFOLLOW??0),0o600);}catch(error){if(error?.code==="EEXIST")throw new Error("guarded FB-4 capture lock already exists");throw error;}try{const pre=kernelLog(),event=firstXid(pre);if(event)throw new Error(`current boot already contains an NVIDIA Xid; reboot before any native capture retry: ${event.trim()}`);const traceName=`${FB4_CAPTURE.tracePrefix}-${randomUUID()}.json`,environment={...process.env,LIMINA_ASSET_ROOT:repo,[FB4_CAPTURE.authorityEnvironment]:portable(repo,authorityAbsolute),[FB4_CAPTURE.traceEnvironment]:traceName};for(const key of Object.keys(environment))if(/TIMESTAMP/i.test(key))delete environment[key];const engineArgs=["--window",...(options.fullscreen?["--fullscreen"]:[]),"--width",String(authority.presentation.minimumResolution[0]),"--height",String(authority.presentation.minimumResolution[1]),"--frames","1",FB4_CAPTURE.module],execution=await buildCaptureProducerClosure({repoRoot:repo,entryPaths:FB4_CAPTURE.sourcePaths,runtimeBinary:FB4_CAPTURE.binary,argv:[FB4_CAPTURE.binary,...engineArgs],environmentKeys:Object.keys(environment)}),bootId=(await readFile("/proc/sys/kernel/random/boot_id","utf8")).trim();await guarded(resolve(repo,FB4_CAPTURE.binary),engineArgs,environment,repo);const post=firstXid(kernelLog());if(post)throw new Error(`NVIDIA Xid detected after capture; stop immediately and do not retry until reboot: ${post.trim()}`);const tracePath=resolve(traceRoot,traceName),traceBytes=await readFile(tracePath),artifact=JSON.parse(traceBytes.toString("utf8"));await unlink(tracePath);validateNativeArtifact(artifact,authority);await mkdir(dirname(out),{recursive:true,mode:0o700});await chmod(dirname(out),0o700);const staging=`${out}.staging-${process.pid}`;await mkdir(staging,{recursive:false,mode:0o700});try{const outputs=[];for(const capture of artifact.captures){const submission=capture.renderSubmission,resources=capture.rendererResources;if(submission?.scope!=="single-production-frame-all-passes"||submission.drawCalls<=1||submission.triangles<=1||resources?.scope!=="renderer-live-after-production-frame")throw new Error(`FB-4 ${capture.id} lacks whole-scene telemetry`);const rgba=Buffer.from(capture.rgbaBase64,"base64");if(portableAssetContentHash(rgba)!==capture.rgbaContentHash)throw new Error(`FB-4 ${capture.id} RGBA hash drifted`);const sanity=pixels(rgba,capture.width,capture.height),png=await sharp(rgba,{raw:{width:capture.width,height:capture.height,channels:4}}).png({compressionLevel:9,adaptiveFiltering:true}).toBuffer();if(!png.subarray(0,8).equals(PNG))throw new Error("FB-4 PNG encoding failed");const privatePath=resolve(staging,`${capture.id}.png`);await writeFile(privatePath,png,{flag:"wx",mode:0o600});outputs.push({id:capture.id,path:portable(repo,resolve(out,`${capture.id}.png`)),width:capture.width,height:capture.height,pngSha256:raw(png),pngByteLength:png.length,rgbaContentHash:capture.rgbaContentHash,pixelSanity:sanity});}const evidence={...artifact,captures:artifact.captures.map(({rgbaBase64,...capture})=>capture),guardEvidence:{schema:"limina.nvidia-xid-guard/v1",preflight:{xidObserved:false},live:{redundantPollMs:250,xidObserved:false},postflight:{xidObserved:false}},reviewBridge:{artifactDirectory:FB4_CAPTURE.reviewRoot,bindHost:FB4_CAPTURE.reviewBindHost,public:false,staged:false,candidatePrefix:prefix},outputs},evidenceBytes=Buffer.from(`${JSON.stringify(evidence,null,2)}\n`),evidencePath=portable(repo,resolve(out,"capture-evidence.json"));await writeFile(resolve(staging,"capture-evidence.json"),evidenceBytes,{flag:"wx",mode:0o600});const exact=async(entry)=>{const bytes=await readFile(resolve(repo,entry.path));return{...entry,contentHash:portableAssetContentHash(bytes)}},provenance=buildFb4CaptureProvenance({captureEvidence:{path:evidencePath,sha256:raw(evidenceBytes),contentHash:portableAssetContentHash(evidenceBytes)},trace:{sha256:raw(traceBytes),byteLength:traceBytes.length},subject:{candidateId:manifest.candidateId,manifest:{path:authority.candidate.manifest.path,sha256:authority.candidate.manifest.sha256,contentHash:portableAssetContentHash(manifestBytes)},glb:authority.candidate.glb,reviewAuthority:{path:portable(repo,authorityAbsolute),sha256:raw(authorityBytes),contentHash:portableAssetContentHash(authorityBytes)},irHash:authority.candidate.irHash},environment:{authority:await exact(authority.environment.authority),runtimeBundle:await exact(authority.environment.runtimeBundle),shot:authority.environment.shot,context:authority.environment.context},execution,gpuSafety:{bootId,timestampQueriesEnabled:false,xidObserved:false,preflightSource:"journalctl-kernel-current-boot",liveFollower:"journalctl-kernel-follow-current-boot",redundantPollMs:250,postflightSource:"journalctl-kernel-current-boot"},outputs:outputs.map(({pixelSanity,...output})=>output)});await writeFile(resolve(staging,"capture-provenance.json"),`${JSON.stringify(provenance,null,2)}\n`,{flag:"wx",mode:0o600});await rename(staging,out);return{evidencePath,provenancePath:portable(repo,resolve(out,"capture-provenance.json")),outputs,reviewBindHost:FB4_CAPTURE.reviewBindHost,staged:false};}catch(error){await rm(staging,{recursive:true,force:true});throw error;}}finally{await lock.close();await unlink(lockPath).catch(()=>{});}}
const value=(args,flag)=>{const i=args.indexOf(flag);if(i<0||!args[i+1])throw new Error("usage: bun tools/preview/run-native-fb4-multi-room-capture.mjs --authority <json> --out-dir <new-dir> --review-prefix <prefix>");return args[i+1];};
if(import.meta.url===`file://${process.argv[1]}`){const args=process.argv.slice(2);console.log(JSON.stringify(await runFb4Capture({authorityPath:value(args,"--authority"),outDir:value(args,"--out-dir"),reviewPrefix:value(args,"--review-prefix"),fullscreen:args.includes("--fullscreen")}),null,2));}
